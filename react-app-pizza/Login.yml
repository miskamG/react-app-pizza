info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: '{{domain}}pizza-api-demo/auth/login'
  body:
    type: json
    data: |-
      {
        "email": "ar4@gmail.com",
        "password": "123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

info:
  name: Products
  type: http
  seq: 1

http:
  method: GET
  url: '{{domain}}pizza-api-demo/products?limit=1000&offset=0'
  params:
    - name: limit
      value: '1000'
      type: query
    - name: offset
      value: '0'
      type: query
    - name: name
      value: олив
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
